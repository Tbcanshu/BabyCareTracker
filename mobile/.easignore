# node_modules (EAS installs fresh on server)
node_modules

# Expo internal
.expo
.expo-shared

# Git
.git
.gitignore
.gitattributes

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
dist
build
coverage

# Android BUILD ARTIFACTS (huge compiled .so/.a files - regenerated on EAS)
# NOTE: android/ root must be included for bare workflow, but build caches must not
android/.gradle
android/build
android/app/build
android/app/.cxx

# Media files
*.mp4
*.mov
*.avi
*.mkv

# Backend (separate project)
../backend